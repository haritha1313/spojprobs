#include <iostream>
#include<math.h>
#include<iomanip>
using namespace std;

int main() 
{
	int t;
	float v,s,a,h;
	cin>>t;
	while(t--)
	{
		cin>>v;
		a=cbrt(4*v);
		h=(a/(sqrt(3)));
		s=(1.5*a*a*(sqrt(3)));
		std::cout << std::fixed << std::setprecision(10) <<s<<endl;
	}
	
	return 0;
}
